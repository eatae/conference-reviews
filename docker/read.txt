db_data
    - хранение базы данных из контейнера

nginx
    - конф. для nginx
    - Dockerfile

php-cli
    - Dockerfile


----------------------


Show:
    con:8080
    (if change hosts file)

Enter php-cli:
    docker exec -it con-cli bash

Docker-Compose up:
    docker-compose up --build -d
    docker-compose up -d


